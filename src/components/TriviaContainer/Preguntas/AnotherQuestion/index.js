import React from 'react';
import PropTypes from 'prop-types';
import Preguntas from './../../Preguntas'

const AnotherQuestion = ({anotherQuestions}) => (
  <div className="pregunta">{anotherQuestions}</div>
)
